import React from 'react';
import { Text } from 'react-native';

const hiStyle = {
    color: '#777',
    fontWeight: '600',
    fontSize: 50,
};

export const StartScreen = () => <Text style={hiStyle}>Hi!</Text>
